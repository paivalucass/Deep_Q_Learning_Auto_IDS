from setuptools import setup, find_packages

setup(
    name='DQL_IVN_IDS',
    version='1.0',
    packages=find_packages(where='src'),
    package_dir={'': 'src'},
    author='Lucas Paiva',
    # description='',
)